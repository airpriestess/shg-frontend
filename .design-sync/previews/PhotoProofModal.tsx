import React from 'react';
import { PhotoProofModal } from 'shg-frontend';
export function Open() {
  return <PhotoProofModal open onClose={() => {}} onSaved={() => {}} threadId="t1" audioTitle="Money Finds Me First" />;
}
