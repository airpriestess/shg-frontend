import React from 'react';
import { AudioVault } from 'shg-frontend';
export function Screen() {
  return <AudioVault userTier="goddess" onCreateThread={() => {}} onPlayAudio={() => {}} playingId={null} onUpgrade={() => {}} />;
}
