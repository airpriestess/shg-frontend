import React from 'react';
import { Dashboard } from 'shg-frontend';
export function Goddess() {
  return <Dashboard userTier="goddess" onNavigate={() => {}} onAddProof={() => {}} onCreateThread={() => {}} />;
}
