name: Diagnose audio
on:
  push:
    branches: [diag-audio]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: List bucket
        run: curl -s https://shg-audio-worker.airpriestess.workers.dev/_list
      - name: Check each track link
        run: |
          while read -r u; do
            [ -z "$u" ] && continue
            h=$(curl -s -o /dev/null --max-time 3 -w "%{http_code}" "$u" || true)
            echo "$h  $u"
          done < .github/audio-urls.txt
